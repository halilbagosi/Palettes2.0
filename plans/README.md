# Implementation Plans

Generated by the improve skill on 2026-07-15, against commit `9726b96` **plus
the uncommitted working tree on branch `icloud-sync`** (each plan's drift
check covers this). Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row when done.

Environment: this project builds only on a machine with Xcode. Executors
without `xcodebuild` do the file work and report verification as pending.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Test baseline: PalettesTests target + shared scheme + first suites | P1 | M | — | TODO |
| 002 | Persistence hardening: dropped edits, orphan deletion, swallowed saves | P1 | M | 001 | TODO |
| 003 | Replace palette parallel arrays with `PaletteColor` struct | P1 | M–L | 001 (land after 002) | TODO |
| 004 | Release APNs environment for CloudKit sync | P2 | S | — | TODO |
| 005 | Hex parser: accept 3/4/8-digit input | P2 | S | 001 | TODO |
| 008 | DX & hygiene: CI, lint, CLAUDE.md, xcuserdata, README | P2 | M | 001 (test job only) | TODO |
| 009 | Coolors-style palette export (code/URL/SVG, then ASE/PDF) | P2 | L | 001; adapt if 003 landed | TODO |
| 006 | Cache ColorNamer Lab table | P3 | S | 001 | TODO |
| 007 | Generation error handling (cancellation vs failure) | P3 | S | — (tests need 001) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002 and 003 both modify `Palettes/App/AppData.swift` — execute sequentially (002 first) to avoid conflicts.
- 001 must precede anything that adds tests; it creates the target, shared scheme, and the suites others extend.
- 009 builds on the palette model — if 003 has landed, use `paletteColors`; the plan covers both shapes.
- 004, 007, and 008 (minus the CI test job) are independent and can run any time.

## Findings considered and rejected

- **Whole-table fetch on every debounced persist / `RunLoop.main` debounce scheduler**: real but irrelevant at personal-library scale; revisit only if libraries reach thousands of entries. Correctness fixes (002) on the same code come first.
- **GenerationOrbView continuous TimelineView + Metal shader cost**: needs on-device Instruments to size; this machine can't run it. Recorded, not planned.
- **Prompt-injection hardening of the generation `vibe` string**: guided generation (`@Generable`) plus downstream hex validation already bound the damage to an odd palette name; not worth a plan.
- **ColorsView/PaletteView pairwise duplication (extract generic LibraryScreen)**: highest maintenance tax in the repo but L-effort and MED-risk while the same views are being actively rewritten on `icloud-sync`; re-plan after the branch settles and 001–003 land.
- **`ImageColorExtractor` premultiplied-alpha skew for semi-transparent pixels**: negligible for photos; not planned.
- **Compatibility/ shims as tech debt**: intentional (iOS 17 target + iOS 26 APIs); deletable only when the deployment target reaches 26.
- **`build/` committed / stale**: false alarm — `build/` is gitignored and untracked.
